/// <reference types="react" />
import type { GiscusProps } from './types';
export default function Giscus({ id, repo, repoId, category, categoryId, mapping, term, reactionsEnabled, emitMetadata, inputPosition, theme, lang, loading, }: GiscusProps): JSX.Element | null;
